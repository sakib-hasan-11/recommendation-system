{
  "jobDefinitionName": "recommendation-training-job",
  "type": "container",
  "containerProperties": {
    "image": "ACCOUNT_ID.dkr.ecr.REGION.amazonaws.com/recommendation-system:training",
    "vcpus": 8,
    "memory": 32768,
    "jobRoleArn": "arn:aws:iam::ACCOUNT_ID:role/recommendation-job-role",
    "environment": [
      {
        "name": "AWS_REGION",
        "value": "us-east-1"
      },
      {
        "name": "LOG_LEVEL",
        "value": "INFO"
      }
    ],
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "/aws/batch/recommendation-training",
        "awslogs-region": "us-east-1",
        "awslogs-stream-prefix": "ecs"
      }
    }
  },
  "retryStrategy": {
    "attempts": 2,
    "evaluateOnExit": [
      {
        "onStatus": "Task*",
        "onReason": "*OutOfMemory*",
        "onExitCode": "1"
      }
    ]
  },
  "timeout": {
    "attemptDurationSeconds": 86400
  }
}
